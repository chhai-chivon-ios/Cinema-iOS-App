# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

use_frameworks!

target 'Cinema-iOS-App' do
	pod 'Alamofire', '~> 4.4'
	pod 'SwiftyJSON'
  	pod 'Kingfisher'
end
